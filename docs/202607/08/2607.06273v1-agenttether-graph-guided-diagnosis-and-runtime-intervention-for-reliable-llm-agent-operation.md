---
title: "AgentTether: Graph-Guided Diagnosis and Runtime Intervention for Reliable LLM Agent Operation"
title_zh: AgentTether：面向可靠LLM智能体操作的图引导诊断与运行时干预
authors: "Chenyu Zhao, Shenglin Zhang, Wenwei Gu, Yongqian Sun, Dan Pei, Chetan Bansal, Saravan Rajmohan, Minghua Ma"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06273v1"
tags: ["query:agent-error"]
score: 9.0
evidence: 诊断智能体轨迹中的错误并执行运行时干预
tldr: "LLM代理在多步工具任务中可靠性低，现有恢复方法缺乏诊断。AgentTether通过构建依赖感知的Critical Transition Graph，结合离线正常行为模型定位失败子轨迹，并利用修复记忆生成行为指导，可选进行运行时干预。在261个tau-bench任务上，Banking领域修复了59%的Qwen3.7-max失败任务和65%的GPT-5.4失败任务，同时减少重试轮次和令牌消耗。该框架无需修改代理或环境，可作为实用可靠性层部署。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06273v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1500, \"height\": 508, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06273v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 713, \"height\": 747, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06273v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 733, \"height\": 402, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06273v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1853, \"height\": 834, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06273v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 735, \"height\": 602, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06273v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1807, \"height\": 798, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06273v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 865, \"height\": 193, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06273v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 710, \"height\": 196, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06273v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 912, \"height\": 293, \"label\": \"Table\"}]"
motivation: 现有LLM代理修复方法（如盲目重试、自我反思）无法准确诊断失败原因，导致重复错误或无效恢复。
method: 将每次运行抽象为Transition Units并构建依赖感知的Critical Transition Graph，结合离线正常行为模型和运行时图检测定位失败子轨迹，再通过跨迭代修复记忆生成行为指导，可选施加运行时干预。
result: "在tau-bench的Banking领域，AgentTether将Qwen3.7-max的首次失败任务修复率提升至59.04%（49/83），GPT-5.4至65.12%（56/86），并减少代理轮次和令牌消耗。"
conclusion: AgentTether提供一种无需重新训练的实用修复方法，通过图引导诊断和记忆驱动的恢复，显著提升LLM代理的可靠性，可轻松集成到现有部署中。
---

## 摘要
大语言模型（LLM）智能体越来越多地用于多步骤、有状态工具使用任务，但其生产环境可靠性仍然有限。与静态软件修复不同，智能体修复必须恢复动态轨迹，其中早期决策可能传播到后续错误和外部状态变化。现有自动修复方法仅解决部分问题：盲目重试缺乏诊断，结果反馈仅能判断运行是否失败但无法定位失败位置或原因，而自反思往往缺乏可靠证据以防止相同失败再次发生。我们提出AgentTether，一个运行时修复框架，可在不修改底层智能体或环境的情况下，自动进行运行后诊断和引导式恢复。AgentTether将每次运行抽象为转换单元，通过依赖感知的关键转换图链接它们，并结合离线正常行为模型与运行局部图检测器定位失败关键子轨迹。然后，它将定位的原因转化为基于跨迭代修复记忆的行为范围引导，并可选择应用受保护的运行时干预以在重新执行期间保持修正。同一设计即可部署为离线诊断与引导工具，也可作为在线修复层。我们在三个领域的261个tau-bench任务上使用Qwen3.7-max评估AgentTether，并在Banking领域使用GPT-5.4测试跨模型迁移。在最困难的Banking领域，AgentTether修复了59.04%（49/83）初始失败的Qwen3.7-max任务和65.12%（56/86）初始失败的GPT-5.4任务。总体而言，AgentTether在提高修复效果的同时减少了智能体轮次和端到端方法令牌数，表明其可作为实用的可靠性层，能够封装现有智能体部署，减少无效重执行，并在无需重新训练智能体的情况下提高恢复能力。

## Abstract
Large language model (LLM) agents are increasingly used for multi-step, stateful tool-use tasks, yet production reliability remains limited. Unlike static software repair, agent repair must recover dynamic trajectories whose early decisions can propagate into later errors and external state changes. Existing automatic remedies address only part of this problem: blind retry adds no diagnosis, outcome feedback says whether a run failed but not where or why, and self-reflection often lacks grounded evidence to prevent the same failure from recurring. We present AgentTether, a run-time repair framework that automates post-run diagnosis and guided recovery without modifying the underlying agent or environment. AgentTether abstracts each run into Transition Units, links them through a dependency-aware Critical Transition Graph, and localizes failure-critical subtrajectories by combining an offline normal-behavior model with a run-local graph detector. It then converts the localized cause into behavior-scoped guidance backed by cross-iteration Repair Memory, and can optionally apply guarded run-time intervention to keep the correction active during re-execution. The same design can be deployed as an offline diagnostic-and-guidance tool or as an online repair layer.   We evaluate AgentTether on 261 tau-bench tasks across three domains with Qwen3.7-max, and test cross-model transfer on Banking with GPT-5.4. On the hardest Banking domain, AgentTether repairs 59.04% (49/83) of initially failed Qwen3.7-max tasks and 65.12% (56/86) of initially failed GPT-5.4 tasks. Overall, AgentTether improves repair effectiveness while reducing agent turns and end-to-end approach tokens, suggesting a practical reliability layer that can wrap existing agent deployments, reduce wasted re-execution, and improve recovery without retraining the agent.

---

## 论文详细总结（自动生成）

好的，以下是对论文《AgentTether: Graph-Guided Diagnosis and Runtime Intervention for Reliable LLM Agent Operation》的详细中文总结。

### 1. 论文的核心问题与整体含义

- **研究动机**：大型语言模型（LLM）智能体在执行多步骤、有状态的工具使用任务时，其生产环境可靠性是主要瓶颈。现有自动修复方法存在严重缺陷：
    - **盲目重试**：没有诊断，可能重复同样的错误。
    - **结果反馈**：仅告知运行是否失败，无法定位具体原因和位置。
    - **自反思**：往往缺乏有依据的证据，导致修复无效或重复失败。
- **核心问题**：如何自动化地诊断LLM智能体失败轨迹的根本原因，并引导其在下一次运行时成功恢复，同时避免过度干预和修复退化。
- **整体含义**：提出一种可靠、可部署的运行时修复框架，旨在通过“诊断-引导-干预”的闭环，提升现有LLM智能体的可靠性，且无需修改智能体本身或进行重新训练。

### 2. 论文提出的方法论

- **核心思想**：AgentTether 通过抽象、图形化和状态化的方式模拟人类操作员的修复循环，即：**理解失败原因 → 生成具体行为指导 → 在重执行中持续监督并适时干预**。
- **关键技术细节**：
    1.  **抽象与图形化**：将智能体的一次运行轨迹抽象为**转换单元**的序列，并构建**关键转换图**。该图不仅包含时间顺序边，还包含因共享工件、错误签名等产生的**依赖边**，从而捕捉早期间接错误传播。
    2.  **图引导诊断**：
        - 在关键转换图上进行根因分析。
        - 使用两个互补检测器定位异常子轨迹：
            - **离线HGT检测器**：基于异构图的正常行为模型，识别偏离常见成功模式的结构。
            - **实时检测器**：基于隔离森林的轻量级检测器，识别当前轨迹中的局部异常。
        - 检测结果通过分析师LLM总结为**根因、转折点和恢复提示**。
    3.  **状态化行为引导**：
        - 将诊断结果转化为**行为范围引导**，包括修正指令和注入计划（如禁止某些工具、提醒执行特定步骤）。
        - **修复记忆**跨迭代记录哪些指令已解决、哪些待解决，防止重复修复或修复倒退。
    4.  **受保护的运行时干预**：
        - 在智能体重执行期间，通过**检查钩子**监控其行为。
        - 检测**循环重复、意图漂移、期望偏差**和**遗忘行为**。
        - 仅在必要时，通过追加工具结果或插入合成用户消息的方式，注入轻量级修正，并设有**冷却机制、最简干预**等保护措施。

### 3. 实验设计

- **数据集/场景**：使用 **τ-bench** 基准测试，包含三个日益复杂的领域：
    - **零售**：电子商务订单管理（114个任务）
    - **航空**：航班预订操作（50个任务）
    - **银行**：金融交易与合规（97个任务）
    共计261个任务。基准测试评估过程级正确性，而非仅最终结果。
- **对比方法**：
    - **盲目重试**：基线，重试不加入任何反馈。
    - **结果反馈**：仅提供最终奖励（成功/失败）作为反馈。
    - **反思**：使用智能体自身的自我反思作为反馈。
    - **AgentTether (仅后处理)**：包含图引导的诊断和引导，但禁用运行时干预。
    - **AgentTether (完整)**：包含所有组件。
- **模型**：主智能体为 **Qwen3.7-max**，并在银行领域使用 **GPT-5.4** 进行跨模型迁移验证。所有辅助角色使用 **DeepSeek-V4-Pro**。

### 4. 资源与算力

- 论文中**未明确说明**训练使用的具体GPU型号、数量、训练时长等信息。
- 提到了离线HGT模型是在外部成功轨迹语料库（约21,143个）上训练的，训练参数为：**隐藏维度52，20个epoch，batch size 128**。该训练量级相对较小，表明方法对算力需求不高。
- 在线干预的隔离森林检测器需要更少的资源。

### 5. 实验数量与充分性

- **实验数量**：论文进行了充分的实验，包括：
    - **主对比实验**：在所有三个领域（261个任务）上对比了5种方法。
    - **跨模型迁移实验**：在银行领域对GPT-5.4进行了对比。
    - **组件消融实验**：分析每个核心组件（离线HGT、修复记忆）的贡献。
    - **运行时干预效果分析**：对比有无干预的修复效果及对智能体行为的影响。
    - **诊断效果评估**：评估图引导诊断的准确性。
- **充分性与客观性**：
    - **实验设计公平**：所有方法在相同初始失败任务上执行，共享相同的辅助角色模型，参数固定。
    - **结果稳健**：使用McNemar精确检验评估统计显著性。
    - **覆盖全面**：覆盖了不同领域、不同模型、不同组件，验证了通用性。
    - 实验充分且客观地对比了与先前方法的差异，并解释了每个组件的必要性。

### 6. 论文的主要结论与发现

- AgentTether **显著优于**所有对比基线：
    - 在261个初始失败任务中，AgentTether修复了**69.11%** 的任务，相比盲目重试提升**26.02个百分点**。
    - 在最困难的银行领域，修复率从盲目重试的**26.51%** 提升至**59.04%**（Qwen3.7-max）和**65.12%**（GPT-5.4）。
- **组件贡献明确**：图引导诊断和修复记忆是核心贡献；运行时干预在**银行领域**具有**显著的统计学增益**（+12.05 pp），尤其能在长轨迹、重合规的任务中防止智能体偏离正确路径。
- **效率提升**：AgentTether **减少了智能体轮次和端到端方法令牌**，同时提高了修复率，表明其引导和干预能**避免无效试探**。
- **跨模型转移**：方法对Qwen3.7-max和GPT-5.4都有效，表明其**模型无关性**。

### 7. 优点

- **方法论创新**：
    - 首次将**图形根因分析、记忆驱动的行为引导和运行时受保护干预**统一集成到一个无需改造智能体的修复框架中。
    - 使用**依赖感知的关键转换图**而非简单时间序列进行诊断，能捕捉早期间接错误。
    - **受保护干预**通过冷却、最小化等机制，实现了**可控且非破坏性的纠正**。
- **实验设计精良**：
    - **公平对比**：所有方法在相同初始失败任务上运行，使用第三方辅助模型，消除自我评估偏差。
    - **充分消融**：通过移除核心组件清晰展示其贡献。
    - **跨领域/模型验证**：增强了结论的泛化能力。
- **实用价值**：
    - **修复效率**：提升了修复效果，同时**降低了计算成本**（更少的轮次和令牌）。
    - **无需修改或重新训练**：可直接作为**轻量级可靠性层**部署在现有智能体之上。
    - **可配置性**：可作为纯诊断/引导工具或在线干预层使用。

### 8. 不足与局限

- **辅助判断依赖**：诊断和分析严重依赖辅助LLM（分析师和验证器），虽然设计了保护措施，但并未完全消除对其质量的依赖。
- **过度干预风险**：在少数情况下（银行领域的3个任务），运行时干预可能**过度干预**，导致智能体重新规划而非正确执行，平衡适时纠正与过度控制是一个开放性问题。
- **额外延迟**：运行时干预会增加**墙上时钟时间**（虽未大幅增加智能体轮次），对于对延迟敏感的生产环境可能构成挑战。
- **评估局限性**：仅在τ-bench基准测试上评估，其任务设计、工具生态与真实生产环境可能存在差距。未来需要在代码生成、多智能体协调等更复杂场景中验证。
- **实验资源不透明**：未提供训练离线模型所需的详细算力信息，增加了可复现性成本。

（完）
