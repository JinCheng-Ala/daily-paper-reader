---
title: "Diffusion-Guided Search via Exponential Tilting (DiffTilt): An Application to Falsification of Safety-Critical Systems"
title_zh: 通过指数倾斜的扩散引导搜索 (DiffTilt)：在安全关键系统反例验证中的应用
authors: "Tanmay Khandait, Preetom Biswas, Hideki Okamoto, Bardh Hoxha, Georgios Fainekos, Giulia Pedrielli"
date: 2026-07-25
pdf: "https://arxiv.org/pdf/2607.23134v1"
tags: ["query:agent-error"]
score: 8.0
evidence: 通过扩散引导搜索发现安全关键系统失败，检测轨迹错误
tldr: 现有安全关键系统故障检测方法因乘性稀缺效应而效率低下。本文提出DiffTilt框架，通过指数倾斜扩散模型生成的场景与轨迹联合分布，以KL最优方式将概率质量重分配至故障相关行为。该方法在ARCH-COMP基准及新拖拉机-挂车任务上取得竞争或更优的伪造性能，尤其当规范定义不限于STL公式时优势更大。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23134v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 580, \"height\": 418, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23134v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 914, \"height\": 684, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23134v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 912, \"height\": 1082, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23134v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1510, \"height\": 364, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23134v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 913, \"height\": 653, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23134v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1885, \"height\": 630, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23134v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1311, \"height\": 657, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23134v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1693, \"height\": 342, \"label\": \"Table\"}]"
motivation: 现有条件采样方法因场景与轨迹的双重稀缺而搜索代价高昂。
method: 通过指数倾斜扩散模型联合分布实现重要性采样，引导概率向故障区域迁移。
result: 在多个基准上实现竞争或更优的伪造效果，尤其在非STL规范下增益显著。
conclusion: DiffTilt将昂贵仿真限于学习评分函数，实现有效且可复用的故障搜索。
---

## 摘要
发现自主与信息物理系统中罕见的安全关键故障是验证与确认中的一个基本挑战。现有的反例验证方法依赖于条件采样策略，这些策略将环境与系统执行轨迹的联合分布进行分解，因此受到乘法稀有性效应的影响：同时缺乏导致故障的输入和导致故障的执行轨迹，使得穷举搜索代价高昂。本文提出了DiffTilt，一个分布框架，通过指数倾斜扩散模型诱导的环境与执行轨迹联合分布。我们证明，扩散引导采样在联合空间中具有重要性采样的精确解释，其中引导分数引发概率质量向故障相关行为的KL最优重新分配。进一步证明，倾斜可以显著放大故障概率，并在性能上严格优于受乘法稀有性限制的条件采样。在该框架中，联合生成模型作为可重复使用的场景先验，无需忠实表示被测系统。昂贵的系统仿真则被限制用于学习一个表征场景质量的评分函数，从而实现其选择性和自适应使用。我们在ARCH-COMP基准测试上研究了DiffTilt，并提出了一个额外的牵引车-拖车基准测试，展示了当场景生成由明确规范而非奖励引导时几种方法的行为。与最先进方法相比，所提方法实现了具有竞争力或更优的反例验证性能，当规范定义不限于STL公式时，性能提升更大。

## Abstract
Discovering rare safety-critical failures in autonomous and cyber-physical systems is a fundamental challenge in verification and validation. Existing falsification approaches rely on conditional sampling strategies that factor the joint distribution over environments and system executions, and therefore suffer from multiplicative rarity effects: the simultaneous scarcity of failure-inducing inputs and failure-inducing traces makes exhaustive search prohibitively expensive.   This paper develops DiffTilt, a distributional framework that exponentially tilts a diffusion model-induced joint distribution over environments and executions. We show that diffusion-guided sampling admits an exact interpretation as importance sampling in the joint space, where guidance scores induce a KL-optimal reallocation of probability mass towards failure-relevant behaviors. We further show that tilting provably amplifies failure probability and strictly outperforms conditional sampling, which is limited by multiplicative rarity. In this framework, the joint generative model serves as a reusable prior over scenarios and need not faithfully represent the system under test. Expensive system simulations are instead limited to learning a scoring function that characterizes scenario quality, enabling their selective and adaptive use.   We study DiffTilt on ARCH-COMP benchmarks, and we propose an additional tractor-trailer benchmark showing the behavior of several approaches when scenario generation is guided by a well-defined specification rather than a reward. The proposed method achieves competitive or improved falsification performance compared to state-of-the-art approaches, with larger gains when specification definition is not limited to STL formulas.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：在自主与信息物理系统中，发现罕见的安全关键故障（反例生成）是验证与确认的基本挑战。现有方法基于条件采样策略（先采样输入场景，再采样系统执行），这种分解策略导致**乘性稀有性效应**：故障输入本身罕见（概率 δ << 1），给定输入后故障轨迹也罕见（概率 ε << 1），整体故障概率为 δε 乘积，样本复杂度高达 Ω(1/(δε))，即使条件生成模型完美也难以高效搜索。
- **整体含义**：本文提出 DiffTilt 框架，将反例验证问题重新解释为**对扩散模型诱导的联合分布（环境 × 轨迹）进行指数倾斜**，从而将概率质量 KL‑最优地重新分配到故障相关区域。该方法在理论上有严格保障，在实验上对长时域、嵌套及非 STL 规范表现出显著优势。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：
  - 学习一个联合扩散模型 \(p_\theta(x, y)\) 作为可复用的先验（输入 \(x\) 与轨迹 \(y\) 的联合分布）。
  - 定义评分函数 \(S(x, y)\)（如鲁棒性预测、奖励、专家定义成本）作为故障倾向的代理。
  - 通过**指数倾斜**构造倾斜分布：\(p_{\beta}(z) \propto p_\theta(z) \exp(\beta S(z))\)，其中 \(\beta\) 为倾斜参数。
  - 证明该倾斜分布是满足期望评分约束的最小 KL 散度分布（Proposition 2），且当评分函数满足一阶随机占优（FOSD）时可严格放大故障概率（Theorem 1）。
- **关键技术细节**：
  - **联合扩散建模**：使用 JointNet 架构，两个 UNet 分支分别处理输入和轨迹，通过零初始化跳跃连接实现交叉条件。
  - **梯度引导反向扩散**：利用 Tweedie 去噪估计 \(\hat{z}_0\)，在每个反向步骤中对噪声预测添加梯度项：\(\epsilon^{(k)}(z_t, t) = \epsilon_\theta(z_t, t) - \gamma_t \beta_k \nabla_z S_k(\hat{z}_0)\)，从而将采样偏向高评分区域。
  - **选择与仿真**：从 \(M\) 个候选场景中按指数权重 \(p_i \propto \exp(\beta_k S_k(x_i, y_i))\) 采样一个 \(x_i\)，执行真实仿真器 \(\Phi(x_i)\) 并评估真正鲁棒性 \(R(x_i, \Phi(x_i))\)。若发现故障则返回，否则将新数据加入数据集并更新评分函数。
- **算法流程（Algorithm 1）**：离线训练联合扩散模型 → 在线迭代：先验采样初始化数据集 → 训练评分网络 → 用梯度引导反向扩散生成 \(M\) 个候选 → 按权重选择并仿真 → 更新评分函数 → 直到找到故障或预算耗尽。

### 3. 实验设计
- **数据集/场景**：
  - **ARCH‑COMP 标准基准**：
    - Automatic Transmission（AT）：8 个 STL 规范（速度、转速、齿轮保持等）。
    - Chasing Cars（CC）：6 个 STL 规范（跟车距离、嵌套时序条件等）。
  - **新引入的 Tractor‑Trailer（TT2D）基准**：6 个难度递进的场景（无障碍/单障碍/宽/窄停车/随机障碍），使用目标可达性的否定作为 STL 规范。
- **对比方法**：**FReaK**——基于 Koopman 算子的线性化代理伪造方法，是 ARCH‑COMP 中的顶尖工具。
- **实验配置**：遵循 ARCH‑COMP 协议，仿真预算为 \(B + 30\)（AT/CC，\(B\in\{4,8,16\}\)）或 \(B+200\)（TT2D，\(B=16\)）；所有结果基于 10 次独立运行；报告伪造率（FR）和成功伪造所需平均仿真次数（S）；TT2D 还报告平均运行时间（\(\bar{T}_f\)）和标准误差。倾斜参数 \(\beta\) 通过粗略搜索选定（AT: 20, CC: 100, TT2D: 10）。

### 4. 资源与算力
- **离线训练**：联合扩散模型在**单个 NVIDIA A100 GPU** 上训练 **300 个 epoch**，耗时 **不到 4 小时**。该开销可被多个下游规范复用（无需为每个规范重新训练）。
- **在线阶段**：未明确说明 GPU 型号或数量，仅提及在 A100 上运行。
- 参数规模、数据集大小等细节未给出。

### 5. 实验数量与充分性
- **实验数量**：AT 上测试了 8 个规范，CC 上 6 个规范，TT2D 上 6 个场景。每个配置 10 次独立重复。还比较了两种预算（500 点和 4000 点训练数据）。
- **充分性**：
  - 👍 覆盖了多种系统（线性的、混合的、非线性的）、多种规范类型（简单、嵌套、长时域）以及离散和连续动力学。
  - 👍 与最先进基线（FReaK）公平对比，严格遵循 ARCH‑COMP 协议。
  - 不足之处：**缺乏消融实验**（如不同 \(\beta\) 值的系统敏感度、不同评分函数形式的影响、不同扩散步数的效果）。AT 上部分规范（AT1、AT51、AT54）完全失败，且论文承认这些情况是梯度平坦导致，但未深入分析或设计对策。
  - 总体而言，实验是充分且客观的，但未完全覆盖所有变量。

### 6. 论文的主要结论与发现
- **理论结论**：
  - 指数倾斜是 KL‑最优的重加权，能严格放大故障概率当且仅当评分函数对故障样本一阶随机占优于非故障样本（Theorem 1）。
  - 条件采样受限于乘性稀有性（即使在完美条件模型下），而联合倾斜可突破此限制（Corollary 2）。
  - 对确定性系统，联合倾斜等价于沿着动力学流形的输入空间最优重要性采样（Proposition 4）。
- **实验结论**：
  - DiffTilt 在 CC 的长时域、嵌套规范（CC4, CC5, CCx）上显著优于 FReaK（仿真次数减少 3–11 倍，伪造率达 1.0）。
  - 在 TT2D 全部 6 个场景上达到 100% 伪造率，而 FReaK 在困难场景（S2, S6）仅 10%–30%；即使成功时 DiffTilt 所需仿真和运行时间也更少（S1: 19.8 vs 98.1 次仿真，238s vs 3131s）。
  - 在 AT 上 DiffTilt 表现不如 FReaK，尤其对离散信号（AT5）和含梯度平坦的规范，反映出方法对梯度信息的依赖。
- **整体优势**：训练一次联合先验后可在多个规范间复用；评分函数可任意定义（不限于 STL），使方法适用于规划、合成等更多任务。

### 7. 优点
- **理论扎实**：将扩散引导反例验证严格建模为重要性采样，给出 KL‑最优性和故障概率放大的充分条件。
- **分离先验与评分**：生成模型不须准确匹配真实系统，只需作为近似先验；昂贵仿真仅用于训练评分函数，实现样本高效。
- **黑盒兼容**：不需要微分系统模型或 STL 解析形式，适用任何可评估的评分函数。
- **可复用性**：离线训练一次的联合先验可在不同规范间重复使用而无需重训练。
- **实验设计**：新引入的 TT2D 基准可系统调节难度，有利于清晰分析方法行为。

### 8. 不足与局限
- **梯度平坦问题**：对离散或混合动力学（如变速箱齿轮切换），评分函数梯度近乎零，导致引导失效，方法退化为未引导的随机采样。论文承认此局限但未提出解决方案。
- **倾斜参数 \(\beta\) 的敏感性**：\(\beta\) 为固定值，仅通过粗略搜索确定，未进行系统调优或自适应调度。过大 \(\beta\) 会放大模型误差，可能产生离流形样本。
- **模型不匹配风险**：理论保证建立在先验分布下，当先验与真实分布有显著差距时，倾斜可能放大错误区域，论文仅提及此 trade‑off 但未给出定量刻画。
- **计算开销**：DDPM 推理需要 1000 步，联合架构较慢，限制了实际部署；文中也指出需要更高效的采样器（如 DDIM）和更快架构。
- **缺少随机系统验证**：理论分析假定确定性动力学（推论 4），实验也均为确定性系统。对随机系统（如噪声传感器）的扩展仅提及为未来工作。
- **消融实验不足**：未比较不同扩散步数、不同评分函数结构、不同候选数 \(M\) 的影响，也未测试在线更新评分函数的频率影响。

（完）
