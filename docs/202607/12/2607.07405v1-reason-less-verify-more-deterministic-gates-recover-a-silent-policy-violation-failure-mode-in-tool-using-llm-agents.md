---
title: "Reason Less, Verify More: Deterministic Gates Recover a Silent Policy-Violation Failure Mode in Tool-Using LLM Agents"
title_zh: 少依赖推理，多进行验证：确定性门控恢复工具使用型LLM智能体中一种无声的策略违规故障模式
authors: "Vikas Reddy, Sumanth Reddy Challaram, Abhishek Basu"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07405v1"
tags: ["query:agent-error"]
score: 9.0
evidence: 检测LLM智能体轨迹中的静默策略违规失败
tldr: "工具型LLM代理在执行任务时可能静默违反部署策略，产生无错误提示的错误状态。本研究在τ²-bench航空领域发现78%的失败为此类问题，并提出确定性只读预执行门控机制，在写操作前检查调用合法性。在gpt-4o-mini上，门控将完整基准成功率从29.6%提升至42.0%（+12.4pp），并在另一组种子集上重现（+12.3pp）。该机制在触发任务上提升显著，非触发任务上效果不显著；负控制实验表明其在策略宽松时有效。贡献在于确定性门控可预防一类静默策略违反写操作，为可靠LLM代理提供轻量级干预。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.07405v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1849, \"height\": 420, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07405v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 875, \"height\": 167, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07405v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1849, \"height\": 205, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07405v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1850, \"height\": 228, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07405v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1850, \"height\": 234, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07405v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1851, \"height\": 277, \"label\": \"Table\"}]"
motivation: 工具型LLM代理在策略宽松环境下，工具可执行违禁操作而不报错，导致静默错误状态，亟需轻量级预防措施。
method: 设计确定性只读预执行门控，在执行写操作前检查工具调用与当前状态是否违反域策略。
result: "在gpt-4o-mini上，四门控套件使完整基准成功率提升12.4个百分点（29.6%→42.0%），且分布在25/36触发任务上提升19.2pp。"
conclusion: 确定性门控不能保证任务成功，但可在行为边界上确定性地预防一类静默策略违反写操作，提升LLM代理可靠性。
---

## 摘要
工具使用型LLM智能体可能在看似成功完成任务的同时，违反其被部署所执行的策略。在策略许可的环境中，即使领域策略禁止相应的状态转换，工具也可能执行任何格式正确的调用。结果是出现一种无声的错误状态（如预订被取消、乘客数量被更改、索赔未经核实即被执行），这种状态既不被工具也不被智能体的自我报告所暴露。我们在$τ^2$-bench航空领域研究这种故障模式。在一个预算有限的智能体上，78%的观测故障是无声的错误状态故障，且没有工具错误；总故障率在不同种子间可重现，并非采样噪声。随后我们评估了一种轻量级干预措施：确定性的只读预执行门控，在允许写入之前检查提议的调用和当前状态。一套四个门控组将gpt-4o-mini上的全基准成功率从29.6%提升至42.0%（提升12.4个百分点；配对任务级bootstrap P=0.0012），并且在另一个15个种子的独立集合上重现了提升（提升12.3个百分点；P=0.0008）。效果集中在门控触发的地方：在26/50个触发任务中，成功率提升了19.2个百分点，而在24个未触发任务中的变化不排除零。两个负对照（自我执行的零售领域和BFCL）界定了该机制：当工具是策略许可时，门控有帮助；而在工具已经自我执行的地方，门控作用不大。作为提示性证据而非核心主张，相同的故障模式在前沿模型中依然存在：默认推理下的gpt-5.2仍尝试策略违规写入，相同的门控组将成功率从61.2%提升至71.6%（提升10.4个百分点；P=0.020；n=5，未进行复现）。贡献在于一个有界评估和可靠性结果：确定性门控并不能保证任务成功，但可以在动作边界上确定性地防止已知类别的无声策略违规写入。

## Abstract
Tool-using LLM agents can violate the very policies they are deployed to enforce while appearing to complete the task successfully. In policy-permissive environments, a tool may execute any well-formed call even when the corresponding state transition is forbidden by domain policy. The result is a silent wrong state (a booking cancelled, a passenger count changed, a claim acted on without verification) that neither the tool nor the agent's self-report exposes.   We study this failure mode in the $τ^2$-bench airline domain. On a budget agent, 78% of observed failures are silent wrong-state failures with no tool error, and the aggregate failure rate is reproducible across disjoint seeds, not sampling noise. We then evaluate a lightweight intervention: deterministic, read-only pre-execution gates that inspect the proposed call and current state before allowing a write. A four-gate suite raises full-benchmark success from 29.6% to 42.0% on gpt-4o-mini (+12.4pp; paired task-level bootstrap P=0.0012), and the lift reproduces on a disjoint 15-seed set (+12.3pp; P=0.0008).   The effect is concentrated where the gates fire: on the 26/50 firing tasks, success rises by +19.2pp, while movement on the 24 non-firing tasks does not exclude zero. Two negative controls (a self-enforcing retail domain and BFCL) bound the mechanism: gates help when tools are policy-permissive and add little where tools already self-enforce. As suggestive evidence, not a central claim, the same failure mode persists at the frontier: gpt-5.2 at default reasoning still attempts policy-violating writes, and the same suite improves success from 61.2% to 71.6% (+10.4pp; P=0.020; n=5, no replication). The contribution is a bounded evaluation and reliability result: deterministic gates do not guarantee task success, but they can deterministically prevent a known class of silent policy-violating writes at the action boundary.

---

## 论文详细总结（自动生成）

# 论文《少依赖推理，多进行验证：确定性门控恢复工具使用型LLM智能体中一种无声的策略违规故障模式》详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：工具使用型LLM智能体（LLM agent）在策略宽松（policy-permissive）的环境中，可能执行违反领域策略的操作而不触发任何错误信号（无声错误状态），导致系统状态被无声破坏，但智能体自身报告任务成功。例如，智能体执行取消不可退款的预订、修改不可变的乘客数量、未经核实就处理索赔等操作。这类故障难以被操作者从运行日志中检测，因此是一个信任问题（trust problem）而非单纯的能力问题。

- **研究背景**：现有基准如τ²-bench中的工具被故意设计为策略宽松：只要调用参数格式正确，即使状态转换被领域策略禁止，工具也会执行。策略仅存储在自然语言文档中由模型遵守，但模型可能遗漏或误用。作者在τ²-bench航空领域发现78%的观测故障属于无声错误状态故障，且这些故障并非采样噪声，在不同随机种子间可重现。

- **整体含义**：论文揭示了一种被忽视的可靠性漏洞，并提出一种轻量级、确定性的干预方法——预执行门控（pre-execution gates），在写操作前检查调用与当前数据库状态，阻止违规写入，从而提升任务成功率并保证特定违规操作一定被阻断。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：在智能体执行写工具调用之前，插入确定性、只读的预执行门控。每个门控是一个纯函数，读取当前数据库状态和提议的工具参数，返回“允许”或“拒绝”。拒绝时，工具调用被短路，智能体收到结构化拒绝消息，可重新规划。门控不涉及LLM调用、不写入状态、不使用真实标注信息。

- **关键技术细节**：
  - 门控定义：$g(\text{tool\_name}, \text{args}, \text{db\_state}) \rightarrow \{\text{allow}, \text{reject}\}$。
  - 门控套件按顺序评估，第一个拒绝的门控获胜并返回原因。所有门控都允许时，原调用正常执行。
  - 设计为**故障开放（fail-open）**：如果门控自身抛出异常，记录错误并允许原调用，避免新引入的误阻断。
  - 需要两个环境补丁：1）预调用分发器拦截提议调用；2）评估回放清洗——将拒绝的调用从记录的动作历史中删除，以匹配基准评估器在干净环境中重放动作的机制。
  - 对应的工具策略规则必须可被状态确定性判定（state-decidable）。

- **门控套件**：主要配置为**四个门控**（在预算模型上选择，固定跨模型层级）：
  1. `cancellation_eligibility`：检查取消预订是否合法（基于票价、时间、保险、已飞航段）。
  2. `baggage_allowance`：检查行李更新是否错误定价或移除允许的行李。
  3. `passenger_count`：检查是否尝试修改不可变的乘客数量。
  4. `must_read_before_write`：检查当前会话中是否已读取过要写入的记录。

## 3. 实验设计：数据集/场景、基准、对比方法

- **主要数据集/场景**：τ²-bench航空领域（airline domain），共50个任务。该基准满足五个必需属性：结构化工具调用、策略宽松工具、状态可判定策略、最终状态评估、能诱发违规的任务分布。
- **基准**：使用τ²-bench的官方评估器，成功定义为 `reward_info.reward > 0`。
- **对比条件**：
  - **预算模型层**：gpt-4o-mini智能体 + gpt-4.1用户模拟器，温度0。
    - 无门控（vanilla）条件 vs. 四门控套件条件。
    - 另有一个六门控最优套件作为预算层上界参考（非主要配置）。
  - **前沿模型层（提示性证据）**：gpt-5.2智能体 + gpt-4.1用户模拟器，默认推理级别。
    - 同样对比 vanila 和四门控套件。
  - **负对照（boundary analysis）**：
    - τ²-bench零售领域（工具自强制执行前置条件）。
    - BFCL v4 multi_turn_base（调用错误返回结构化错误而非静默状态损坏）。
- **指标**：pass 1 平均成功率、τ-bench unbiased pass k 估计器（衡量可靠性）、配对任务级bootstrap置信区间和p值（20,000次重采样）。

## 4. 资源与算力

- **明确说明**：论文未提供使用的GPU型号、数量、训练时长等算力信息。所有实验均基于现成API调用（gpt-4o-mini、gpt-4.1、gpt-5.2），未涉及模型训练。门控本身无模型调用，仅CPU上的确定性计算。因此算力消耗极小，但具体数值未报告。

## 5. 实验数量与充分性

- **主要实验（预算模型）**：
  - 原始实验：50任务 × 5试验 = 250 trials/条件。
  - 复现实验：15个独立种子 × 10试验? 实际描述为“disjoint 15-seed replication”即15个种子，每个种子可能5试验? 文中写“15-seed replication uses disjoint seeds and three times as many trials”（15种子 × 15试验? 实际从表2看：复现n=15，表示每个任务15个试验，即750 trials/条件）。复现证实了12.3pp的提升，与原始12.4pp高度一致。
  - 补充分析：按门控是否触发分层；pass k曲线；每个门控的精确审计（表6）和移除效应；消融实验测试每个门控的贡献。
- **前沿模型实验**：50任务 × 5试验 = 250 trials，无复现，仅提示性。
- **负对照（零售领域）**：8任务 × 8试验? 实际描述8 tasks, 64 trials per condition（推测8×8=64）。
- **负对照（BFCL）**：200 entries multi_turn_base。
- **充分性评价**：
  - **预算模型主要结果经过复现（种子层面），统计显著，支持核心主张**。
  - 门控审计表（precision、removal delta）提供了因果分解。
  - 缺少**留出任务集**上的评估（仅为相同任务集的不同种子），可能对任务集过拟合；但作者承认此局限并指出主要门控的100%精度（161次触发）部分反驳了任务拟合。
  - 未能找到第二个正面领域，导致结论依赖单一领域，泛化性受限。
  - 前沿模型实验未复现，置信区间下限接近零，仅作提示。

## 6. 论文的主要结论与发现

1. **静默策略违规是预算模型中的主要失败类别**（78%），且在不同种子间可重现。
2. **四门控套件显著提升成功率**：gpt-4o-mini上从29.6%提升至42.0%（+12.4pp），p=0.0012；复现集上提升12.3pp（p=0.0008）。
3. **提升集中在门控触发任务**：26个触发任务中提升19.2pp（p=0.0006）；24个未触发任务移动5.0pp但不排除零，说明机制有效。
4. **门控提升可靠性**：pass k曲线显示静默违规是随机而非一贯的；门控将pass 5从8.0%提升至26.0%，提升超过3倍。
5. **关键门控为cancellation_eligibility**（100%精度，移除后成功率下降），其他门控中must_read_before_write精度78%但提升较小；个别门控精度差需修正。
6. **典型欺骗性任务（#48）**：门控能阻止基于用户虚假上下文的违规取消，成功率从0/16提升至16/16。
7. **门控触发不保证恢复**：示例任务#39门控多次触发但任务仍0/5，说明后阻断行为依赖模型。
8. **前沿模型提示性证据**：gpt-5.2仍尝试违规写入，门控带来10.4pp提升（p=0.020），但无复现且下限接近零。
9. **负对照验证边界**：在工具自执行的零售领域和BFCL上，门控无效或略有害，符合预期。

## 7. 优点：方法或实验设计上的亮点

- **问题识别新颖**：识别出“静默策略违规”这一未被广泛强调的可靠性与信任故障类，区分了“大声错误”与“无声错误状态”。
- **干预设计轻量且可验证**：确定性门控无LLM调用，成本极低；其行为完全可重现、可审计，与反射、输出护栏、LLM评判器等随机方法本质不同。
- **实验设计严谨**：
  - 在预算模型上采用 **复现性设计**（15种子复制），证明结果是系统性的而非偶然。
  - **分层分析**：按门控是否触发分解，确认机制与效果因果关系。
  - **负对照**：通过零售领域和BFCL明确界定方法适用边界。
  - **门控精确审计**（precision、removal delta）揭示每个门控的质量与贡献，诚实报告不均匀贡献。
  - **pass k指标**：测量可靠性而非仅平均成功，揭示门控减少随机性。
- **开源承诺**：代码和门控断言将在发表时发布，便于复现和扩展。
- **讨论局限诚实**：承认单一领域、前沿模型证据薄弱、门控可能产生回归、需要审计等，增强可信度。

## 8. 不足与局限

- **单一正面领域**：所有结论建立在τ²-bench航空域；作者承认未能找到满足全部五个轴心的第二个正面基准，限制了泛化性。
- **前沿模型证据仅提示性**：gpt-5.2实验未复现，n=5/任务，置信区间下限接近零，不能作为统计锚。
- **门控质量不均**：一个门控承担大部分提升，一个门控精度差；需要针对每个策略和模型进行审计，不可盲目套用。
- **无恢复保证**：门控只阻断违规写，后阻断恢复依赖模型能力；示例任务#39显示可能循环失败。
- **与现有方法对比缺失**：未测试强提示工程、反射机制、Output guardrails等基线能否同等提升；特别是对于非欺骗性失败，仅比较了无干预 vs 有门控。
- **可能任务过拟合**：门控在相同任务集上设计并评估，复现仅针对种子而非任务；尽管cancellation_eligibility的100%精度减轻部分担忧，但不能完全排除。
- **拒绝消息的混淆作用**：门控返回的结构化原因本身是恢复信号，无法分离阻断本身与信息反馈的效果。
- **状态可判定性限制**：对需要歧义消解、法律判断或人类判断的策略，门控不适用；方法有明确边界。
- **未报告算力消耗**：未提供API调用成本估计或计算时间，可能对部署成本评估不足。

（完）
