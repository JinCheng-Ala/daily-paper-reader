---
title: "Fail-Fast, Restart-Smart: Early Failure Prediction and Restart for SWE Agentic Tasks"
title_zh: 快速失败，智能重启：SWE智能体任务的早期失败预测与重启
authors: "Chenyu Wang, Yunbo Lyu, Junda He, Zhou Yang, Chenxing Zhong, Yaniv Harel, David Lo"
date: 2026-08-04
pdf: "https://arxiv.org/pdf/2608.03222v1"
tags: ["query:agent-error"]
score: 9.0
evidence: 使用轻量监控器对软件工程代理轨迹进行早期失败预测
tldr: "软件工程（SWE）代理以长轨迹解决仓库级问题，失败运行往往更长且充满冗余探索或循环，导致成本随上下文积累急剧上升。本文提出FailFast-RestartSmart两阶段控制器：轻量级0.6B监视器FailFast仅凭可观察前缀预测失败，不需要策略隐状态；一旦报警，RestartSmart启动无先前提示历史的同策略新滚动，并可将中断的仓库差异作为可选覆盖供代理应用或丢弃。在SWE-bench Verified上，仅用Qwen3.6-27B训练的监视器可迁移至其他策略，节省14.6%-20.4%执行令牌；在25%假阳性率下将Qwen3.6-27B解决率从66.6%提升至71.8%，远超冷重启的66.8%。结果表明，早期失败检测与顺序同策略恢复能兼顾效率与解决率，优于逐步骤停止策略。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.03222v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1631, \"height\": 727, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03222v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 860, \"height\": 333, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03222v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 883, \"height\": 972, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03222v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 795, \"height\": 332, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03222v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 879, \"height\": 331, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03222v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 862, \"height\": 260, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03222v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 823, \"height\": 702, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03222v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 882, \"height\": 365, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03222v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 883, \"height\": 330, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03222v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 734, \"height\": 280, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03222v1/table-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 882, \"height\": 335, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03222v1/table-012.webp\", \"caption\": \"\", \"page\": 0, \"index\": 12, \"width\": 750, \"height\": 673, \"label\": \"Table\"}]"
motivation: 软件工程代理以长轨迹解决仓库问题，失败运行更冗长且冗余，提前终止可能误伤成功轨迹，但失败轨迹或含有效编辑。
method: 提出两阶段控制器FailFast-RestartSmart：轻量级0.6B监视器FailFast仅从轨迹前缀预测失败，触发后RestartSmart启动无先前历史的同策略新滚动，并附上中断的仓库差异供代理决定应用或丢弃。
result: "在SWE-bench Verified上，监视器跨策略迁移，节省14.6%-20.4%执行令牌；25%假阳性率下将Qwen3.6-27B解决率从66.6%提至71.8%，冷重启仅66.8%。"
conclusion: 结论表明，基于可观察前缀的早期失败检测与顺序同策略恢复相结合，可在不牺牲性能的前提下显著节省计算资源，优于冷重启和逐步骤停止方法。
---

## 摘要
软件工程（SWE）智能体通过长轨迹解决仓库级别的问题，随着上下文积累，这些轨迹的代价越来越高。失败的运行往往更长，并表现出冗余探索或循环，这表明某些失败可能在完成之前就可检测到。然而，提前终止可能会中断本来会成功的轨迹；相反，不成功的轨迹可能仍包含有用的仓库编辑。我们提出了FailFast-RestartSmart，一种用于单一活动轨迹的两阶段控制器。FailFast是一个轻量级0.6B监控器，使用终止和密集的失败到通过监督进行训练，从可观察的前缀预测失败，而无需策略logits或隐藏状态。在发出警报时，RestartSmart启动一次新的同策略回放，无需先前的提示历史，并将中断的仓库差异作为可选覆盖层提供，智能体可以检查、应用或丢弃该覆盖层。在SWE-bench Verified上，仅使用Qwen3.6-27B轨迹训练的监控器可迁移到其他三种策略，包括一个封闭API模型，并在目标5%误报率下节省14.6%-20.4%的执行令牌；在Qwen3.6-27B上，其20.4%的节省超过了我们逐步AgentStop调整所实现的12.5%。在目标25%误报率下，RestartSmart将Qwen3.6-27B的解决率从66.6%提升至71.8%，而冷重启仅达到66.8%。总之，这些结果支持早期终止与顺序同策略恢复。

## 速览
**TLDR**：SWE代理解决仓库问题时轨迹长且失败代价高。本文提出FailFast-RestartSmart两阶段控制器：轻量监控器从观测前缀预测失败，触发后同策略重启并可选叠加中断的diff。在SWE-bench Verified上节省14.6%-20.4%执行令牌，5%误报率；25%误报率时将Qwen3.6-27B分辨率从66.6%提升至71.8%，验证早期终止与顺序同策略恢复的有效性。 \
**Motivation**：失败轨迹往往更长且冗余探索，早期检测可节省成本，但需避免中断成功轨迹。 \
**Method**：两阶段控制器：FailFast轻量监控器预测失败，RestartSmart以同策略重启并可选叠加中断diff。 \
**Result**：节省14.6%-20.4%执行令牌；分辨率从66.6%提升至71.8%，优于冷重启。 \
**Conclusion**：早期终止配合同策略恢复可显著提升SWE代理效率与成功率。

---

## Abstract
Software engineering (SWE) agents resolve repository-level issues through long trajectories that grow increasingly expensive as context accumulates. Failed runs tend to be longer and exhibit redundant exploration or looping, suggesting that some failures may be detectable before completion. Early termination, however, risks interrupting trajectories that would otherwise succeed; conversely, an unsuccessful trajectory may still contain useful repository edits. We present FailFast-RestartSmart, a two-stage controller for a single active trajectory. FailFast is a lightweight 0.6B monitor trained with terminal and dense fail-to-pass supervision to predict failure from observable prefixes without policy logits or hidden states. Upon an alarm, RestartSmart launches a fresh same-policy rollout without prior prompt history and offers the interrupted repository diff as an optional overlay that the agent may inspect, apply, or discard. On SWE-bench Verified, a monitor trained solely on Qwen3.6-27B trajectories transfers to three other policies, including a closed-API model, and saves 14.6%-20.4% of execution tokens at a target 5% false-positive rate; on Qwen3.6-27B, its 20.4% saving exceeds the 12.5% achieved by our per-step AgentStop adaptation. At a target 25% false-positive rate, RestartSmart raises Qwen3.6-27B resolution from 66.6% to 71.8%, whereas cold restart reaches only 66.8%. Together, these results support early termination with sequential same-policy recovery.

---

## 论文详细总结（自动生成）

## 1. 核心问题与研究动机

软件工程（SWE）智能体通过长轨迹逐步解决仓库级 issue，随着上下文不断累积，每一步都可能重新处理日益膨胀的历史记录，形成 **"令牌雪球效应"**（token snowball effect），计算成本随时间急剧上升。已有研究发现：

- **失败轨迹通常更长**，且表现出冗余探索、重复操作或循环等行为；
- 这些复发行为提示：**失败可能在其完整结束之前就可被检测到**；
- 但提前终止是一个干预问题，而不仅是预测问题——假警报可能打断本来会成功的轨迹，反而损害端到端性能；
- 反过来，终局失败的轨迹并非全无价值：已有研究（如 AgentLens）指出约 54.9% 的失败轨迹属于 **Partial-fail**，即与已知成功流程在结构上接近，包含可恢复的部分进展。

因此，核心研究问题是：**能否在轨迹早期识别大概率失败的运行、及时止损，并将节省的计算资源与轨迹中的部分进展转化为更成功的重试？** 现有工作要么只做离线失败分析、要么只做在线早停、要么做过程中纠正，均未系统研究"顺序式同策略重试 + 跨重启边界的编辑传递"这一场景。

## 2. 方法论

### 2.1 整体框架

提出 **FailFast–RestartSmart**，一个两阶段控制器，作用于单个活跃轨迹：

- **FailFast**：轻量级在线监视器，负责逐步预测失败，在设定误报率（FPR）预算下最大化对失败轨迹的召回；
- **RestartSmart**：当监视器触发警报后，启动一次**无先前提示历史的同策略全新回放**，并将中断轨迹中的仓库编辑作为可选覆盖层（overlay）交给新轨迹处理。

### 2.2 FailFast：轨迹早期终止

**状态表示与训练目标**

- 将任意前缀序列化为两部分：`[ISSUE]`（任务描述） + `[WINDOW]`（最近 8 步的 thought/action/observation，外加最新一次产生补丁步骤的持久化锚点）。
- 仅靠终局二元标签存在稀疏延迟监督问题，因此为每个前缀附加**密集进度目标**：
  - `g^f2p_t`：重放前缀中的 bash 命令，提取中间补丁，由官方评估器计算 fail-to-pass 通过率；
  - `g^p2p_t`：pass-to-pass 通过率；
  - 初始值为 (0,1)（未修改仓库状态），仅在有效编辑时更新，回退工作树时重置。
- 额外构造 **Bradley–Terry 偏好对**：将成功与失败前缀按步数分位段匹配配对，控制实例难度与轨迹进度的影响。

**模型架构与训练损失**

- 骨干：**冻结的 Qwen3-0.6B**，对 attention 与 MLP 投影层施加 LoRA 适配；
- 池化最后一层 token 的隐藏状态后，接三个 fp32 线性头：价值头（终局解决概率）、F2P 头、P2P 头；
- 训练目标：

```
L = BCE(s, y_final) + λ_f·BCE(â_f, g_f2p) + λ_p·BCE(â_p, g_p2p) + λ_r·[-log σ(s+ - s-)]
```

- 实证确定 λ_p = 0（P2P 信号无增益），λ_f = 0.5，λ_r = 0.25。

**校准与部署策略**

- 原始价值 logit 经 Platt scaling 校准；
- 构造 **meta score**：逻辑回归融合校准价值 v、F2P 进度估计和步数比例 φ，用于逐步决策；
- 轨迹级警报：使用 count-M（累计票数）或 sustained-K（连续票数）规则；设置最小步数门槛 φ_min（实验取 20 步），既可避免起始阶段噪声触发，也可在前 20 步完全跳过监视器推理；
- 部署工作点通过**预算约束网格搜索**选定：在目标 FPR 下最大化召回；
- 训练采用 out-of-fold 方式，仅校准与阈值在评估集上拟合，嵌套交叉检验确认无测试标签过拟合。

### 2.3 RestartSmart：保留编辑的智能重启

**提取过往编辑**

- 重放被中断轨迹的 bash 命令，在每条命令后捕获 `git diff HEAD`，当 diff 改变时记录快照，仅保留任务基线已存在的文件（排除代理新建文件）；
- 不严格在警报处 tf 截断：定位 tf 之后（或之前最近的）首个编辑，**延长截断**直至连续 g 步无新编辑，确保捕获的是逻辑完整的补丁而非残缺编辑片段。

**以覆盖层方式传递编辑**

- 将提取的 diff 挂载为**可移除的 git-apply 后端工具**，代理可执行 `overlay diff`、`overlay on`、`overlay off`；
- 覆盖层**默认关闭**，提示语中明确它是"来自被停止尝试的未经验证的线索"，要求代理自行验证、取舍；
- 关键设计：**不将编辑拼入提示文本**。理由：冗长的文本摘要会引发 LLM 锚定效应，使重试代理过早锁定在先前的错误推理路径上；物理 diff 隔离了"可能有效的编辑"和"导致失败的原因"。

## 3. 实验设计

### 3.1 数据集与场景

- **数据集**：SWE-bench Verified（500 个实例，350 训练 / 50 验证 / 100 测试），按实例分组切分，防止跨集泄漏；
- **框架**：mini-swe-agent，官方报告结果与模型自带 SWE-bench 分数吻合良好；
- **轨迹采集**：每个实例 11 次独立运行（种子 0–10），每次最多 100 步。

### 3.2 评估的策略模型

| 模型 | 开放/闭源 | 参数量级 | 原生解决率 |
|---|---|---|---|
| Qwen3.5-9B | 开放 | 9B | 48% |
| Qwen3.6-27B | 开放 | 27B | 66% |
| Gemma4-31B | 开放 | 31B | 62% |
| Gemini 3 Flash | 闭源 API | — | 67% |

### 3.3 对比方法

- **AgentStop**（Pham et al. 2026）：基于 token log 概率、token 数、步骤重复等执行信号，用梯度提升树预测失败（需白盒访问，不适用于闭源）。作者按其释放代码复现，并扩展为逐步骤版本；
- **Duration control**：仅根据轨迹步数判断，无学习信号的上界控制；
- **Cold restart**：由 FailFast 中断后，无任何先前上下文地从零重启；
- **SWE-PRM**（Gandhi et al. 2025）：基于过程奖励模型的提示干预方法，使用其最强配置（Claude-Sonnet-4 作为监督模型）。

### 3.4 实验分组

1. **FailFast 主实验**：单监视器（仅在 Qwen3.6-27B 轨迹上训练）跨四个策略模型评估，FPR 预算为 5%/10%/15%/20%/25%；
2. **RestartSmart 主实验**：各策略使用自己轨迹上训练的策略专用监视器，在 10% 与 25% FPR 工作点评估三种重启策略（RestartSmart / Cold restart / SWE-PRM）；
3. **Abalation 实验**（正文）：
   - 监视器骨干规模：0.6B vs 4B；
   - 输入特征：I（ISSUE）、W（WINDOW）、S（STATE）的消融；
   - 编辑完成耐心窗口（Immediate vs Wait）；
4. **Abalation 实验**（技术附录）：
   - 观察窗口大小 k ∈ {2, 4, 8, 16}；
   - 训练目标：仅 V vs V+F2P vs V+F2P+P2P；
   - 训练种子数 N ∈ {3, 6, 11}；
   - 统计显著性测试（McNemar 检验、bootstrap 置信区间）。

## 4. 资源与算力

论文提供了部分基础设施信息：

- **策略轨迹生成**：SGLang 服务模型，GPU 包括 NVIDIA H100 80GB、A100 40GB、RTX A5000 24GB、RTX 6000 Ada 48GB；节点内存 128–512 GB，Ubuntu 22.04，Docker/Apptainer 容器；
- **监视器训练**：0.6B 骨干 + LoRA，**在 2 张 GPU 上训练不到 6 小时**（PyTorch 2.6 + transformers 5.9 + peft 0.19）；
- **监视器推理**：对评估集的推理只需数分钟，占用 < 2GB VRAM；
- **能耗估算**：0.6B 监视器比 9–31B 策略模型每 token 便宜 15×–52×，监控开销约为总推理计算量的 0.1%–0.6%。

论文未明确说明策略轨迹生成阶段的总 GPU 卡时数或总能耗。

## 5. 实验数量与充分性

**实验量**：主实验覆盖 4 个策略模型 × 5 个 FPR 预算，加 3 个策略 × 2 个 FPR 预算的重启实验，加上 5 组以上消融实验，总体规模较大。

**统计严谨性**：所有对比均在同一 500 条轨迹上配对，使用精确双侧 McNemar 检验与 10,000 次非参数 bootstrap 检验；FailFast 的召回提升在 30 组策略×预算比较中有 25 组显著；RestartSmart 解决率提升在所有策略与预算下均显著（p ≤ 9.4×10⁻³）。

**客观性评估**：实验整体设计较为严谨：
- 同一份轨迹供所有方法共享，配对比较公平；
- Duration control 严格上界了无信号停止的性能；
- 训练-验证-测试按实例分组，无泄漏；
- 嵌套交叉验证确认选择的工作点并非测试标签过拟合产物。

但也有局限：监视器仅在 SWE-bench Verified + mini-swe-agent 框架下评估，跨框架、跨任务类型的推广性尚未验证；SWE-PRM 因无官方代码，只能从论文复现，存在复现偏差风险。

## 6. 主要结论与发现

1. **FailFast 节省计算显著**：5% FPR 下跨四个策略节省 14.6%–20.4% 执行令牌；在 Qwen3.6-27B 上（20.4%）超过 AgentStop（12.5%）和 Duration（11.4%）；计入监控开销后，三个开放权重策略的估算推理能耗与运营碳排放节省约 14.5%–20.3%。
2. **跨策略迁移有效**：仅在 27B 轨迹上训练的监视器可直接迁移到 9B、31B 开放权重模型和 Gemini 3 Flash 闭源 API，无需权重更新，仅需在验证集上校准阈值。
3. **RestartSmart 可提升解决率**：10% FPR 下三个开放策略提升 3.0–4.0 个百分点；25% FPR 下 Qwen3.6-27B 从 66.6% 提升至 71.8%（+5.2 个百分点），冷重启仅 +0.2 个百分点。
4. **编辑覆盖层优于冷重启与提示干预**：冷重启破坏大量原本可通过的轨迹（Qwen3.6-27B 在 10% FPR 下 FP lost 达 36.4%），而 RestartSmart 让强模型自身批判性地评估并利用先前编辑；SWE-PRM 的提示干预对弱模型（9B）有效（+4.2%）但对强模型（27B）反而有害（−3.2%）。
5. **可调节的算力-性能权衡**：用户可通过 FPR 预算选择不同工作点。25% FPR 下 Qwen3.6-27B 获得最大提升（+5.2%，+43.8% token 开销），10% FPR 则是更经济的权衡（+3.2%，+30.3%）。
6. **监视器规模不是关键**：0.6B 与 4B 骨干无显著差异——前缀级失败信号存在规模饱和效应，超轻量级监视器即可达到最佳效果。
7. **编辑完成耐心窗口至关重要**：等待第一次警报后编辑并多观察 5 个无编辑步骤，可将 FP lost 从 18.8%（立即截断）降至 8.8%（25% FPR 下）。

## 7. 优点

- **针对性强**：准确识别了"早停 + 恢复"文献中的空白——顺序式同策略重试，并系统回答了"什么应跨过重启边界"这一关键问题；
- **设计精巧**：
  - 用物理 diff 覆盖层而非文本摘要传递上下文，有效规避 LLM 锚定效应；
  - 覆盖层默认关闭、由代理自主决定，保留了代理的自主性；
  - 密集 F2P 监督解决了稀疏终端标签下的信用分配问题；
- **实用性强**：0.6B 监视器仅需 ~2GB VRAM，对闭源 API 也可应用（仅需可观察文本），且附带能耗与碳节省估算；
- **实验规范**：配对比较、显式 FPR 预算、统计显著性检验、嵌套交叉验证、多个消融维度，整体证据链完整；
- **基线选择用心**：Duration control 严格上界了无信号停止的效果，AgentStop 被改编为逐步骤版本以支持跨方法对比。

## 8. 不足与局限

- **评估范围有限**：仅在 SWE-bench Verified + mini-swe-agent 一个框架上评估，未涉及其他 SWE 任务类型或其他代理框架，泛化性存疑；
- **重启开销偏高**：RestartSmart 的 token 开销（20.5%–43.8%）不低，虽然换来了解决率提升，但对计算资源紧张的用户未必划算；
- **SWE-PRM 复现偏差风险**：该基线无官方代码，仅凭论文描述复现，可能无法完全反映原方法真实性能；
- **监视器训练数据成本**：需要每实例 11 条完整轨迹（最多 100 步）用于训练，轨迹采集本身是一笔不小的一次性开销；
- **编辑提取依赖 git diff 的完整性**：方法假设代理的编辑以 git diff 形式可完整捕获，对于不遵循标准 git 工作流的代理或涉及二进制文件/大量新文件的任务可能失效；
- **闭源 API 评估受限**：对 Gemini 3 Flash 只评估了 FailFast（因为 AgentStop 需要内部 logprobs），无法对比完整基线集合；
- **统计功效**：100 个测试实例 × 5 次运行的规模，在 25% FPR 等较激进预算下，部分比较的置信区间较宽。

（完）
