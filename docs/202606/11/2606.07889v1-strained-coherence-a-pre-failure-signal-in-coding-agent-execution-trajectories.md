---
title: "Strained Coherence: A Pre-Failure Signal in Coding Agent Execution Trajectories"
title_zh: 强迫连贯性：编码代理执行轨迹中的故障前信号
authors: "Marut Pandya, Kasey Zhang, Baiqing Lyu"
date: 2026-06-05
pdf: "https://arxiv.org/pdf/2606.07889v1"
tags: ["query:agent-error"]
score: 9.0
evidence: 提出‘紧张连贯性’作为编码智能体执行轨迹中的预失败信号
tldr: "LLM编码代理有时会承认自身推理问题但仍继续行动，这种现象称为“应变一致”（strained coherence），是安全相关的失败前兆。本文给出操作定义，并基于Qwen3.5-35B-A3B和Claude Sonnet 4.6构建检测器，在Terminal-bench-2轨迹上达到94%失败率预测精度，比话语标记基线高6个百分点。检测器输出可解释的跨度级信息，揭示代理忽略的冲突。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.07889v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 812, \"height\": 265, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07889v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1713, \"height\": 367, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07889v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 740, \"height\": 312, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07889v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1713, \"height\": 364, \"label\": \"Table\"}]"
motivation: LLM代理在推理中识别到问题却仍执行错误行为，构成安全风险，需要检测这种预失败信号。
method: 使用Claude Sonnet 4.6作为裁判，读取完整轨迹并标记应变一致片段，在Qwen3.5-35B-A3B骨干网络上评估。
result: "标记轨迹失败率94%，未标记46%（差距47点，p=0.003）。在Gemma4-31B上方向一致但未显著，高冗长度子集中差距达30-40点。"
conclusion: 应变一致是有效的失败前兆，检测器提供可解释的输出，优于基线，但依赖思维内容可见性。
---

## 摘要
基于LLM的编码代理有时会承认自身推理中存在某个问题，然后仍然继续执行。我们将这种模式称为强迫连贯性：一种与安全相关的故障模式，代理拥有应改变其行为的信息，陈述了该信息，但仍然违背它行动。该模式与言语化奖励破解重叠，即代理指出任务代理与底层目标之间的张力，却仍然优化该代理。我们给出了一个操作性定义，构建了一个Claude Sonnet 4.6评判器，读取完整轨迹并标记模式出现的区间，并使用Qwen3.5-35B-A3B骨干在44条Terminal-bench-2轨迹上进行了评估。被标记的轨迹失败率为94%，而未标记的轨迹为46%（差距47个百分点，Fisher精确检验p=0.003；排除三个提示嵌入示例后差距46个百分点，p=0.006）。在匹配选择性下，检测器达到了94%的精确率，而基于词汇话语标记的基线为88%；两种方法的10条轨迹交集具有100%的失败率（Clopper-Pearson 95%置信区间[69%, 100%]）。我们在Gemma4-31B上使用43条轨迹进行了重复实验：整体信号方向一致但不显著（差距20个百分点，p=0.31），衰减主要由13条零思考内容的轨迹驱动，检测器没有基质可分析。在Gemma的高详细度三分位中，差距为+30个百分点；在Qwen的中等和高详细度三分位中，差距各为+40个百分点。在两个模型中，首次标记出现在经过轨迹时间的中位数83-84%，且二元标记在软化显式冲突标记的改写下仍然存活（8/8条轨迹）。与单变量预测器不同，检测器输出可解释的区间级输出——引用的承认、引用的动作和类型化冲突——显示代理看到了什么以及忽略了什么。

## Abstract
LLM-based coding agents sometimes acknowledge a problem in their own reasoning and then proceed anyway. We call this pattern strained coherence: a safety-relevant failure mode in which an agent has information that should change its behavior, states that information, and still acts against it. The pattern overlaps with verbalized reward hacking, where an agent names a tension between a task proxy and the underlying goal yet optimizes the proxy anyway. We give an operational definition, build a Claude Sonnet 4.6 judge that reads full trajectories and flags spans where the pattern occurs, and evaluate it on 44 Terminal-bench-2 trajectories using a Qwen3.5-35B-A3B backbone. Flagged trajectories fail 94% of the time versus 46% for unflagged trajectories (47-point gap, Fisher's exact p = 0.003; 46 points after excluding three prompt-embedded examples, p = 0.006). At matched selectivity, the detector reaches 94% precision versus 88% for a lexical discourse-marker baseline; the 10-trajectory intersection of the two methods has a 100% failure rate (Clopper-Pearson 95% CI [69%, 100%]). We replicate on Gemma4-31B with 43 trajectories: the overall signal is directionally consistent but not significant (20-point gap, p = 0.31), with attenuation driven largely by 13 trajectories with zero think content, where the detector has no substrate to analyze. In the high-verbosity Gemma tertile, the gap is +30 points; in the mid- and high-verbosity Qwen tertiles, it is +40 points each. The first flag appears at a median of 83-84% of elapsed trajectory time across both models, and the binary flag survives paraphrases that soften explicit conflict markers (8/8 trajectories). Unlike univariate predictors, the detector emits interpretable span-level output -- quoted acknowledgment, quoted action, and typed conflict -- showing what the agent saw and ignored.

---

## 论文详细总结（自动生成）

# 论文详细中文总结：Strained Coherence: A Pre-Failure Signal in Coding Agent Execution Trajectories

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：基于LLM的编码代理在执行任务时，有时会在推理中**明确承认**存在某个冲突或问题（例如工具输出与计划的矛盾、约束与推导的冲突），但随后**仍然采取不解决该冲突的行动**，导致任务失败。作者将这种模式称为**"强迫连贯性"（strained coherence）**。
- **研究动机**：该模式是安全相关的故障子类——代理实际上拥有可改变行为的信息，明确陈述了该信息，却违背其行动。它与**言语化奖励破解**（verbalized reward hacking）重叠：代理指出任务代理与底层目标之间的张力，却仍然优化代理。可检测这种模式有助于：
  - 定位可追溯的失效点（trace-visible failures）；
  - 提供可解释的实例级输出（引用具体承认语句和后续动作）；
  - 作为后期干预的触发信号。
- **背景**：现有工作多关注代理故障分类（如TRAIL、AgentErrorTaxonomy、MAST）或事后归因（Who&When、AgenTracer），但极少研究**识别预测性前兆模式**，尤其是这种"明知故犯"的言语化冲突。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：定义一个可操作的模式，包含两个必要条件：
  1. **明确承认冲突**（acknowledgment）：代理在推理中（通常是think调用）用自然语言陈述一个张力，比如工具输出 vs 计划、两个推理结论之间、约束 vs 推导、明确的不确定性、环境状态 vs 任务前提。承认必须明确可引用。
  2. **不解决地行动**（proceeding without resolution）：承认后立即发出一个未解决该冲突的动作（工具调用、代码编辑、声明结论）。解决冲突需要至少一种：收集可裁决的信息、明确论证一方有效、或修订计划容纳双方。机械地打补丁只是消除表面矛盾，不算解决。
- **冲突类型词汇**：工具输出 vs 计划、推理 vs 推理、约束 vs 推导、不确定性、环境 vs 前提。
- **边缘情况**（不应标记）：保留承诺（hedged commitment）、多轮迭代（reasoning-rich iteration）、信息不全下的理性不确定性。
- **检测器实现**：使用**Claude Sonnet 4.6**作为评判器（不同于两个代理骨干模型族，避免同族盲点），读取预处理后的完整轨迹，输出JSON格式的标记跨度，包含：跨度ID、起止步骤、承认步骤、承认引用、动作步骤、动作引用、冲突类型、置信度1-5、推理说明。
- **预处理**：删除空代理步骤，截断超过2000字符的工具观察。
- **提示设计**：包含操作性定义、边缘规则、冲突类型词汇、三个正面示例（来自Qwen轨迹，构成轻微污染）和三个负面示例（边缘情况）。

## 3. 实验设计：数据集、基准、对比方法
- **数据集**：
  - 主要数据集：**Terminal-bench-2**，命令行编码任务，覆盖软件工程、系统管理、科学计算、安全、数据科学。
  - 代理平台：OpenHands SDK，暴露结构化轨迹（含think工具调用）。
  - 骨干模型：
    - **Qwen3.5-35B-A3B**：44条轨迹（16成功，28失败，基础失败率64%）。
    - **跨模型复制**：**Gemma4-31B**：43条轨迹（26失败，17成功，基础失败率60%）。
- **对比基线**：
  - **轨迹长度**（总步数）
  - **工具错误计数**（含traceback、非零退出等）
  - **思考调用计数**
  - **通用连贯性**（Generic coherence）：另一个Claude Sonnet 4.6评判器，使用相同输入格式但通用提示，不提及强迫连贯性定义或示例。
  - **词汇基线**（Lexical baseline）：在think内容中统计话语标记词（如however, but, contradicts, wait, actually, despite, although, conflict, mismatch等），按中位数分割。
- **评估指标**：
  - 每组失败率（95% bootstrap置信区间）
  - 差距（百分点）
  - Fisher精确检验p值
  - 匹配选择性下的精确率
  - 首次标记出现时间（提前步数）

## 4. 资源与算力
- **文中未明确说明**使用了多少GPU、训练时长、计算资源。检测器是**直接调用Claude Sonnet 4.6 API**进行推理，无需训练。代理轨迹收集使用OpenHands SDK，骨干模型为Qwen3.5-35B-A3B和Gemma4-31B，可能需GPU推理，但文中未提及具体硬件或算力开销。

## 5. 实验数量与充分性
- **主要实验数量**：
  - Qwen数据集：44条轨迹
  - Gemma数据集：43条轨迹
  - 总共87条轨迹，样本量较小。
- **消融/控制实验**：
  - **污染排除**：排除提示中三个正面示例后重新计算差距（Qwen上从47点降为46点，p=0.006，依然显著）。
  - **选择性匹配**：标记数量与词汇基线前16条匹配，检测器精确率94% vs 词汇基线88%。
  - **词汇量分层分析**：按思考字符数分为低/中/高三分位，分别计算差距（Qwen低：-31点反转，中+40，高+40；Gemma高：+30点）。
  - **跨模型复制**：Gemma上整体20点差距不显著（p=0.31），但高词汇量子集+30点。
  - **改写鲁棒性**：对8条轨迹的think内容进行改写（软化话语标记），检测器100%保留标记。
  - **定性示例**：提供了两个详细标记示例（db-wal-recovery, bn-fit-modify）。
- **充分性评价**：
  - 优点：考虑了词汇量混杂（stratisfied analysis）、污染效应、跨模型泛化、多种对比基线、改写鲁棒性。实验设计较为全面，尤其注意了混淆因素。
  - 不足：
    - 样本量小（N=44和43），置信区间宽，Gemma上差异不显著，统计效力有限。
    - 未做人类标注验证（construct validity问题）。
    - 仅使用单一基准（Terminal-bench-2），未跨基准复制。
    - 缺少多元回归控制（作者承认由于样本量限制无法做）。
    - 改写测试仅用同一模型族（Claude），可能高估一致性。

## 6. 论文的主要结论与发现
- **主要结果**（Qwen）：
  - 检测器标记的轨迹失败率**94%**，未标记的**46%**，差距**47个百分点**（p=0.003；排除污染后46点，p=0.006）。
  - 匹配选择性下，检测器精确率**94%**，词汇基线**88%**；两者交集10条轨迹**100%**失败率（95% CI [69%,100%]）。
  - 检测器优于基线：通用连贯性差距35点，轨迹长度、思考计数、工具错误计数差距19-24点。
  - 首次标记出现在中位数**84%**经过时间，约9步剩余。
  - 词汇基线在单变量差距上略高（51点），但选择性不同；检测器在细微案例（词汇不突出）上更优。
- **跨模型复制**（Gemma）：
  - 整体差距**20点**（75% vs 55%），方向一致但不显著（p=0.31）。
  - 衰减主因：13条零思考内容轨迹（自动不标记），高词汇量三分位差距+30点。
- **改写鲁棒性**：8/8条轨迹保留标记。
- **定性发现**：最常见的冲突类型是约束 vs 推导，其次是环境 vs 前提。

## 7. 优点：方法或实验设计上的亮点
- **操作性定义清晰**：给出明确的两组件定义、边缘案例、冲突类型，便于复现和争论。
- **可解释输出**：检测器不仅给出是否标记，还提供引用承认语句、引用后续动作、冲突类型和置信度，有利于人工审查和监控。
- **考虑了混淆因素**：词汇量分层分析揭示检测器仅在有合理推理基质的轨迹上有效；污染排除确认结果非提示示例驱动。
- **跨模型复制**：在另一模型族上验证，增强外部有效性。
- **与多种基线比较**：包括通用连贯性（控制提示特异性）和简单词汇计数（控制表面特征），展示增量价值。
- **改写鲁棒性测试**：证明检测器捕获的是结构关系而非特定词汇。
- **选择性匹配分析**：公平比较不同方法的精确率，避免选择性偏见。

## 8. 不足与局限
- **构造有效性未验证**：没有人类标注来确认检测器标记的确实是强迫连贯性而非其他相关特征（如自疑、长推理、任务难度等）。
- **样本量小，统计效力有限**：主要结果置信区间宽，Gemma上差异不显著（p=0.31），多变量回归不可行。
- **数据集单一**：仅使用Terminal-bench-2，未在其他基准（如SWE-bench、AgentBench）验证。
- **检测器严重依赖思考内容**：零思考轨迹自动被排除，限制了在无显式推理链的代理上的应用。
- **改写测试的局限性**：仅软化话语标记，未测试压缩、结构重构或跨家族改写；改写器和评判器同族可能引入偏差。
- **词汇基线很强**：在Qwen上词汇基线单变量差距51点高于检测器47点；尽管匹配选择性下检测器更优，但简单计数仍是强竞争对手。
- **后期信号**：首次标记出现在84%经过时间，仅能作为晚期干预触发器，无法早期预警。
- **覆盖率低**：约一半失败轨迹未被标记（Qwen上28未标记中13失败），需要补充其他信号（如静默越权、未经验证成功声明）。
- **未做模型无关检测器重写**：检测器提示基于Qwen轨迹开发，在Gemma上性能下降可能源于校准而非缺乏基质。
- **缺乏个体任务类别的控制**：未分析是否仅在某些任务类型上有预测力。

（完）
