---
title: Reinforcement Learning for Computer-Use Agents with Autonomous Evaluation
title_zh: 基于自主评估的计算机使用智能体强化学习
authors: "Marta Sumyk, Oleksandr Kosovan"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24515v1"
tags: ["query:agent-error"]
score: 8.0
evidence: 使用VLM自主评估任务完成情况，从截图中检测错误
tldr: 计算机使用智能体通过感知操作GUI执行用户目标，但强化学习因缺少可扩展奖励信号而困难。本文提出用视觉语言模型自主评估任务完成，将评估器噪声建模为二元通道并校正，用于PPO训练。在macOSWorld、Windows Agent Arena和OSWorld上，校正后的奖励使成功率比零样本平均提升12.6个百分点，比原始评估微调提升5.1个百分点。该方法为GUI环境中的强化学习提供了实用奖励信号。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24515v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 900, \"height\": 516, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24515v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1516, \"height\": 722, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24515v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1633, \"height\": 261, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24515v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1646, \"height\": 302, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24515v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1047, \"height\": 275, \"label\": \"Table\"}]"
motivation: 强化学习训练计算机使用智能体缺乏可扩展的奖励信号，手动设计奖励或标注成本高。
method: 用视觉语言模型作为自主评估器，将其噪声建模为二元通道，推导校正奖励估计器用于PPO优化。
result: 在三个基准上，校正评估奖励使成功率比零样本提升12.6个百分点，比原始评估微调提升5.1个百分点。
conclusion: 自主评估可作为GUI环境中强化学习的实用奖励信号，需显式建模和校正评估噪声。
---

## 摘要
计算机使用智能体(CUAs)通过直接在图形用户界面中感知和行动来执行高级用户目标。然而，对于CUAs而言，强化学习仍然困难，因为开放的桌面环境很少提供可扩展的、机器可读的奖励信号：任务成功通常基于视觉，并且难以用人工设计的奖励函数或密集的手动标签来指定。我们提出了一个RL微调框架，该框架使用自主视觉-语言评估作为GUI代理的可扩展监督信号。给定最终截图和原始指令，视觉-语言模型判断任务完成情况并提供终端反馈，无需在策略优化期间使用任务特定的启发式规则或手动标签。由于自主评估器不完美，我们将其反馈建模为有噪声的二元奖励通道，并为近端策略优化推导出噪声校正的奖励估计器。在macOSWorld、Windows Agent Arena和OSWorld上的实验表明，校正后的评估器奖励优于零样本基线和原始评估器奖励，成功率比零样本性能平均提高12.6个百分点，比原始评估器微调提高5.1个百分点。这些结果表明，当明确建模和校正评估器噪声时，自主评估可以作为GUI环境中RL的实用奖励信号。

## Abstract
Computer-Use Agents (CUAs) execute high-level user goals by perceiving and acting directly within graphical user interfaces. However, reinforcement learning for CUAs remains difficult because open-ended desktop environments rarely provide scalable, machine-readable reward signals: task success is often visually grounded and hard to specify with handcrafted reward functions or dense manual labels.   We propose an RL fine-tuning framework that uses autonomous vision-language evaluation as a scalable supervision signal for GUI agents. Given a final screenshot and the original instruction, a Vision-Language Model judges task completion and provides terminal feedback without task-specific heuristics or manual labels during policy optimization.   Because autonomous evaluators are imperfect, we model their feedback as a noisy binary reward channel and derive a noise-corrected reward estimator for Proximal Policy Optimization. Experiments across macOSWorld, Windows Agent Arena, and OSWorld show that corrected evaluator rewards outperform both zero-shot baselines and raw evaluator rewards, improving success rates by an average of 12.6 percentage points over zero-shot performance and 5.1 points over raw evaluator fine-tuning. These results suggest that autonomous evaluation can serve as a practical reward signal for RL in GUI environments when evaluator noise is explicitly modeled and corrected.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

**核心问题**：计算机使用智能体（CUAs）在强化学习（RL）训练中缺乏可扩展、可靠的奖励信号。桌面GUI环境是开放的、视觉化的，任务成功标准难以用程序化规则或人工标注定义，导致RL难以直接应用。

**研究背景**：
- 现有CUAs（如UI-TARS）在OSWorld上成功率仅约60%，远不够实用。
- 传统RL依赖手动设计奖励函数或任务特定启发式（如DOM检查），无法跨应用、跨操作系统泛化。
- 近期工作提出使用视觉-语言模型（VLM）进行自主任务完成评估，但评估器存在假阳性/假阴性噪声，直接使用会引入偏差。

**整体含义**：本文旨在解决“如何利用不完美的自主评估器为CUAs提供实用奖励信号”这一关键瓶颈，通过显式建模评估器噪声并校正，使RL微调成为可能。

## 2. 论文提出的方法论：核心思想、关键技术细节

**核心思想**：将VLM评估器视为一个二元有噪声奖励通道，通过估计其假阴性率(e+)和假阳性率(e-)，推导出渐近无偏的校正奖励估计器，并集成到PPO中。

**关键技术细节**：
1. **设定**：每个任务i有自然语言指令d_i，最终GUI截图x_{i,T}。评估器输出二元信号˜r_i∈{0,1}。
2. **噪声模型**：定义假阴性率e+ = Pr(˜r=0 | r*=1)，假阳性率e- = Pr(˜r=1 | r*=0)，可分性常数D = 1 - e+ - e- > 0（确保评估器优于随机猜测）。
3. **校正估计器**：由条件期望E[˜r | r*] = e- + D·r*，解得`ˆr_i = (˜r_i - e-)/D`，满足E[ˆr_i | τ_i] = r*_i（渐近无偏）。
4. **实际使用**：从独立校准集估计ˆe+、ˆe-，得到`ˆr♯_i = (˜r_i - ˆe-)/ˆD`。
5. **PPO集成**：终端奖励R_i = γ^T · ˆr_i，使用PPO优化策略π_θ，读者估计值由校正奖励反向传播得到。

**算法流程**（文字说明）：
- 步骤1：收集校准数据（独立于训练任务），计算评估器混淆矩阵，得到ˆe+、ˆe-。
- 步骤2：对每个训练任务，让CUAs与环境交互直到终止，获得最终截图。
- 步骤3：VLM评估器输出原始二元信号˜r。
- 步骤4：应用校正公式得到ˆr，作为终端奖励。
- 步骤5：使用PPO更新策略参数，基于ˆr计算优势函数。

## 3. 实验设计：数据集、基准、对比方法

**数据集**：
- 自建合成任务数据集：涵盖42个应用（每OS），每个应用60个自然语言任务，共7,560个任务。按2/3用于RL微调，1/3用于评估器校准（不可召回）。
- 额外引入三个公开数据集的任务描述：OmniAct、GUI-World、GUIDE，丰富任务分布。

**基准环境**：
- macOSWorld（macOS）
- Windows Agent Arena（Windows）
- OSWorld（Linux/跨平台）

**对比方法**：
- Zero-shot基线（UI-TARS基础模型）
- RL + 原始评估器奖励（˜r），统一模型（跨OS训练）
- RL + 校正评估器奖励（ˆr），统一模型
- RL + 原始评估器奖励，每OS独立模型
- RL + 校正评估器奖励，每OS独立模型（本文最终方案）

**评估指标**：任务成功率（task success rate）

## 4. 资源与算力

**论文中未明确说明**使用的GPU型号、数量、训练时长等具体算力信息。仅提及代码开源（GitHub链接），但未提供硬件配置细节。这是本文在报告完整性上的一个不足。

## 5. 实验数量与充分性

**实验组数**：
- 评估器准确性实验：在三个OS上各报告一个混淆矩阵（共3个）。
- RL微调实验：5种训练配置 × 3个基准 = 15组结果，每组报告成功率。
- 消融实验：比较统一模型 vs. 每OS模型，原始奖励 vs. 校正奖励。

**充分性评估**：
- **充分**：覆盖了三个主要桌面OS，对比了关键变量（奖励类型、模型参数化方式），结果表格清晰。
- **客观性**：采用公开基准（OSWorld等），零样本基线为最新开源模型UI-TARS，对比公平。
- **不足**：
  - 未报告多次运行的标准差或置信区间，缺乏统计显著性检验。
  - 消融仅针对模型参数化，未分析不同校准集大小、不同VLM评估器的影响。
  - 缺少对训练过程中奖励分布变化的监控（非平稳性问题）。

## 6. 论文的主要结论与发现

1. **自主VLM评估可作为RL的实用奖励信号**，只要显式建模并校正噪声。
2. **校正后的奖励（ˆr）在所有基准上均优于原始奖励（˜r）**，零样本基线平均提升12.6个百分点，比原始奖励微调提升5.1个百分点。
3. **每OS独立模型比统一模型表现更好**，尤其对macOS和Windows差距明显（macOSWorld: 0.203 vs 0.144；Windows: 0.442 vs 0.403）。
4. 原始奖励在跨OS设置下可能引入偏差（如OSWorld上原始奖励统一模型0.385 vs 校正统一模型0.399）。
5. 评估器错误率存在OS依赖性（e+和e-不同），需要单独校准。

## 7. 优点：方法与实验设计的亮点

- **理论严谨**：从噪声二元通道出发，推导出渐近无偏校正估计器，公式简洁，可即插即用。
- **实践可行**：无需手动标注或任务特定规则，仅需一个校准集估计错误率，即可用于PPO。
- **多OS验证**：同时评估macOS、Windows、Linux三大平台，展示了方法的泛化能力。
- **消融设计清晰**：对比统一 vs. 独立模型、原始 vs. 校正奖励，有效揭示了噪声校正和模型参数化的影响。
- **数据规模合理**：构建了7240+任务的大规模数据集，并引入外部任务描述，提升多样性。

## 8. 不足与局限

- **假设局限性**：假设评估器错误率固定，但实际可能随任务分布（如UI复杂度、语言表达）或策略变化而漂移。未对非平稳性进行建模或补偿。
- **奖励稀疏性**：仅使用终端二元信号，不提供中间奖励，长任务探索效率低。本文未尝试解决。
- **校准集依赖**：校准质量直接影响校正效果。校准集大小、代表性和任务分布与训练集的一致性未深入探讨。
- **评估器盲区**：VLM可能漏判语义正确但视觉细微的结果，或误判基于表面视觉线索的结果。仅依赖最终截图，忽略过程状态。
- **实验报告不完整**：未提供硬件配置、训练时间、多次运行的标准差、超参数设置等关键细节。
- **应用限制**：仅在三个特定基准上测试，未覆盖实际部署中的多样环境（如不同分辨率、可访问性设置、应用版本变更）。

（完）
