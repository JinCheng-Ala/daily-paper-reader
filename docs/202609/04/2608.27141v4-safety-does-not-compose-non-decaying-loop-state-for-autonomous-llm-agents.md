---
title: "Safety Does Not Compose: Non-Decaying Loop State for Autonomous LLM Agents"
title_zh: 安全性不可组合：自主LLM代理的非衰减循环状态
authors: "Chenhao Wu, Haoxuan Jia, Yang Liu, Yingguang Yang, Yuhan Lin, Chongyang Zhang, Hao Zheng, Yulin Huang, Jianshen Zhang, Yongzhi Qi, Shang Luo, Kefu Xu, Jifeng Zhu, Bin Chong"
date: 2026-08-27
pdf: "https://arxiv.org/pdf/2608.27141v4"
tags: ["query:agent-error"]
score: 9.0
evidence: 证明轨迹范围监控器无法发现证据分散在多次自动循环迭代中的智能体安全错误
tldr: 大型语言模型智能体常以自主循环方式运行，但现有安全监控仅针对单条轨迹，状态在每次迭代后重置，导致跨迭代的碎片化攻击证据无法被捕获。本文证明轨迹级监控器的检测率等于误报率，而保留跨迭代状态的监控器可实现完美分离。提出的LoopHarness在循环层面维护持久且非衰减的安全状态，将未授权不可逆操作的期望次数约束为与地平线N无关的常数，并提供完整评估协议。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.27141v4/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1436, \"height\": 891, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.27141v4/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1418, \"height\": 770, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.27141v4/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1021, \"height\": 548, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.27141v4/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1086, \"height\": 262, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.27141v4/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 990, \"height\": 439, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.27141v4/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1178, \"height\": 777, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.27141v4/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 717, \"height\": 280, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.27141v4/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1464, \"height\": 349, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.27141v4/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1506, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.27141v4/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1382, \"height\": 347, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.27141v4/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1316, \"height\": 1141, \"label\": \"Table\"}]"
motivation: 现有智能体安全监控仅针对单轨迹且状态重置，无法防御证据分散于多次迭代的攻击。
method: 提出LoopHarness，在循环层面维护持久非衰减安全状态，结合介导提交与仲裁检测下限。
result: 将未授权不可逆操作的期望次数上界降为B+m-1+m/δ_M，其中B+m-1由无模型规则决定，常数与N无关。
conclusion: 证明轨迹级安全不可组合，循环级持久状态是智能体安全的关键，并提供评估协议与攻防套件。
---

## 摘要
大型语言模型代理正越来越多地被部署为自主循环。从一个人为设定的目标出发，此类系统会反复发现任务、规划、执行工具调用、验证结果，并在多次无人值守的迭代中持久化状态。然而，广泛使用的代理安全保障机制是针对单条轨迹定义的，其安全状态会在下一条轨迹开始时被重新初始化。我们表明，这是一个组合性失败，而非实现细节。我们的核心结果是一个分离：面对证据分散在多次迭代中的攻击，每个仅限定于轨迹的监视器，无论其表达能力多强，其真阳性率都等于假阳性率，因为它所需的证据永远不会出现在它所见的时间窗口中；而保留跨迭代状态的监视器则可以完美地区分两者。我们进一步表明，采用几何衰减风险评分的明显修复方案是不够的，因为耐心对手必须等待的冷却期是一个不随视界$N$增长的常数。然后，我们提出了LoopHarness，它在循环层面恢复了持久、非衰减的安全状态。在中介提交和仲裁器检测下限$δ_M$下，它将未经授权的不可逆操作的期望数量限制为$B+m-1+m/δ_M$，这是关于$N$的常数，其中$B+m-1$项由无模型规则决定，因此不受完全串通的验证者的影响。我们还给出了一个完整的评估协议，包括在原生Agent-SafetyBench任务上的配对干净与受攻击情节、一个决定性证据仅存在于跨迭代中的外部状态攻击套件、逐模块消融实验，以及一个自适应的白盒红队。

## Abstract
Large language model agents are increasingly deployed as autonomous loops. Starting from one human goal, such a system repeatedly discovers work, plans, executes tool calls, verifies outcomes and persists state across many unattended iterations. The agent safeguards in wide use, however, are defined over a single trajectory, and their safety state is re-initialized when the next trajectory begins. We show that this is a failure of composition rather than an implementation detail. Our central result is a separation: against an attack whose evidence is fragmented across several iterations, every trajectory-scoped monitor has a true-positive rate equal to its false-positive rate, however expressive it is, because the evidence it would need never appears in the window it sees, whereas a monitor retaining cross-iteration state separates the two perfectly. We further show that the obvious repair of carrying a geometrically decaying risk score is insufficient, because the cooling-off period a patient adversary must wait is a constant that does not grow with the horizon $N$. We then present LoopHarness, which restores a persistent, non-decaying safety state at the loop level. Under mediated commits and an arbiter detection floor $δ_M$, it bounds the expected number of unauthorized irreversible actions by $B+m-1+m/δ_M$, a constant in $N$, of which the $B+m-1$ term is decided by a model-free rule and therefore survives a fully colluding verifier. We give a complete evaluation protocol on native Agent-SafetyBench tasks with paired clean and attacked episodes, an outer-state attack suite whose decisive evidence exists only across iterations, per-module ablations, and an adaptive white-box red team.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与研究动机

- **背景**：LLM 代理正从"单轮工具调用"走向"无人值守自主循环"——从一个给定目标出发，系统反复执行"发现任务 → 规划 → 调用工具 → 验证结果 → 持久化状态 → 决定是否继续"这一过程（如软件维护、运维、研究辅助、后台金融等场景）。这类系统有两个区别于单轮使用的关键属性：
  1. 存在**不可逆动作**（转账、发送邮件、删除记录）；
  2. 代理携带**跨迭代持久状态**（记忆库、任务台账、累积上下文），且该状态本身就是攻击面。
- **核心矛盾**：现有主流安全机制（输入清洗、逐调用分类、权限上限、能力令牌、轨迹内回滚等）全部限定在**单条轨迹（trajectory）内**，安全状态在每次迭代开始时被重置（per-trajectory reset，Definition 1）。而**攻击者的进度恰恰通过循环刻意保留的持久通道（记忆、知识库）跨迭代累积**，形成结构性不对称。
- **形式化分离（核心论断）**：对于证据被分散到多次迭代（s-fragmented attack）的攻击，任意轨迹级监控器都无法区分攻击流量与良性流量，其**真阳性率 TPR = 假阳性率 FPR**（Proposition 2）——因为所需证据根本不在它可见的窗口内。即使保留会**几何衰减**的跨迭代风险评分也无济于事，因为耐心攻击者只需等待一个**不随地平线 N 增长的常数冷却期**（Proposition 3）。
- 因此，作者将问题定性为"安全性不可组合"的组合性失败，而非某个实现的缺陷或覆盖率缺口。

## 2. 方法论：LoopHarness

LoopHarness 是位于内部 harness 之外的循环级防御，可与任意单轨迹防御组合，由**五个永不重置的持久组件**按固定阶段顺序（Algorithm 1）构成。每轮状态 σi =（Ξi, c, a, ℓ, Mi, Ri, Ki）中没有任何一项在迭代间重新初始化，且 Ξ、计数器和降级级别被绑定进认证检查点。

- **设计主线（信号分层）**：将"动作影响"、"已确证攻击证据"与"验证器不确定度"严格区分为独立信号：
  - 只有 **grounded attack evidence（已确证攻击证据）** 进入保留风险 Ξ；
  - 只有**规则可判定的循环结构标志（loop-structural flag）** 才能锁存（latch）衰减；
  - 动作影响与验证器不确定度只影响当前迭代的路由选择，**不持久化**，防止正常高影响工作或模型不确定性劣化后续迭代。

- **五大组件**：
  1. **IPM（Intake and Provenance Monitor）**：唯一的新任务入口，六条默认拒绝检查（范围合规、信任级别不超过渠道可担保级别、拒绝条目不得无授权重新进入、授权必须原子化、跨轮次血统可显式委托、不可信渠道无注入/升级/扩权模式）；最多 k 个条目/轮。
  2. **XRC（Cross-iteration Risk Cumulant）**：每轮恰好一次更新保留风险
     Ξi = max(ri, γi·Ξi−1)，其中 γi = 1（若任何结构标志已触发）否则 γ↓∈(0,1)。
     高连续风险分本身永不锁存衰减；只有规则可判定的结构标志（附录 D 列出的整组离散事件）能锁存。不锁存时保留只会推迟门限放开；锁存后被动等待永远无法放开闸门。
  3. **MIG（Memory-Integrity Guard）**：追加式日志 + 链式 HMAC 标签 hj = HMACk(⟨j⟩‖⟨ej⟩‖hj−1)；定期检查点认证"链摘要+长度+治理计数器"；单调信任写门（降级自动允许，升级必须带签名授权）；重补水（rehydration）时重验链并做按密钥信任单调性审计；**召回策略只回灌有界后缀的可信条目、排除逐轮摘要**，用以封堵 T9 自喂养记忆投毒。
  4. **ASA（Adversarially Robust Stopping Arbiter）**：三层证据——**无模型调用的确定性门**（不可逆提交 + 结构标志/授权违规/外部认证与工具证据矛盾时触发）+ **去相关检查器**（每轮最多一次模型调用，在廉价/高级两个层级间路由；高级角色必须与 writer 不同模型；只有看到"带参数与授权上下文的尝试动作 + 先前攻击证据或成功提交"才算检出）+ **窗口化发散统计量**（需要超阈值、至少两个独立发散且已有 grounded 攻击证据）。任何检出触发第 6 阶段停机。
  5. **CRG（Compounding-Risk Governor）**：硬预算 B（risk-charged 不可逆提交）与 Badv（高级验证调用）；能力上限是当前 Ξ 的五级特权阶梯函数；执行任务契约（风险清算门限 θw、完成锚点次数上限）；超过上限或连续 K 轮只提交未完成良性工作即强制停机并升级到操作员；回滚只恢复日志内容而**永不重载治理计数器**。

- **核心理论保证**：
  - 定理 5（与地平线无关的伤害上界）：在假设 A1（中介化、有界提交，两轮治理测试间至多 m 个提交）、A2（检测下限 δM：每次含 contract-invisible 未授权提交的迭代被仲裁器以至少 δM 概率标记并停机）、A3（计数器单调不减）下，
    **U_vis ≤ B+m−1（必然成立）；E[U_inv] ≤ m/δM；E[U1:N] ≤ B+m−1+m/δM**——关于 N 的常数。
  - 命题 6：授权代理是纯规则计算（无模型输出作操作数），因此确定性部分 B+m−1 对**任何检查器**（包括被对抗指令完全串通的 writer 模型）都成立，只有 m/δM 项依赖检查器；剩余类恰是"声明范围与操作者真实意图之间的差距"，收紧 A/Π 可把质量移出该类，使 π 归操作者而非对手控制。

## 3. 实验设计

- **基准**：基于 **Agent-SafetyBench**（2,000 任务），保留可执行模拟器且至少含一个工具的任务；规范队列为 **200 个良性、完全可执行、需工具调用的任务**，指令/环境/工具 schema 不变，经确定性筛选与语义审计冻结为 manifest。OOD 评估分割用 97 个父聚类（每配置每地平线 485 个攻击情节，N∈{5,10,20} 共每配置 1,746 条记录）。
- **攻击套件**：A1–A5 继承自单轨迹 SafeHarness 评估（仅保持编号，未重跑）；正式评估从 A6 开始：
  - **Track A（A6–A10）**：外状态干预 + sentinel 检测（sentinel 出现在已执行不可逆调用的参数中即视为攻破），保留轨迹内证据，用于测量随 N 的复合曲线（Track-A 保留性测试）；
  - **Track B（A11–A15）**：**无词汇线索**的外部状态攻击——动作与目的地本地合理、任务为原生任务，违规只有结合跨迭代来源/拒绝/授权/记忆完整性/血统状态才可见（A13
